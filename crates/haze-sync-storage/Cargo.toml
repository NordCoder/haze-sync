[package]
name = "haze-sync-storage"
description = "Storage schema metadata and passive database row models for Haze Sync."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
path = "src/lib.rs"

[features]
default = []
test-support = ["dep:sqlx"]

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["serde", "std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlx = { version = "0.7", optional = true, default-features = false, features = ["postgres", "runtime-tokio-rustls"] }

[lints]
workspace = true
