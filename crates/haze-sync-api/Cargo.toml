[package]
name = "haze-sync-api"
description = "Placeholder crate for future API contracts and route surfaces."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
hex = "0.4"
haze-sync-common = { path = "../haze-sync-common" }
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
subtle = "2.5"

[dev-dependencies]
serde_json = "1.0"

[lints]
workspace = true
