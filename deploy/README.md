# Deployment scaffold

This directory currently contains only a local Docker Compose scaffold for PostgreSQL. Server, Google Drive adapter, reverse proxy, secrets, and production deployment wiring are intentionally deferred to later phases.

Do not place real credentials or OAuth files in this repository.
